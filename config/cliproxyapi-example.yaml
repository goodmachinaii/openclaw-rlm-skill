# CLIProxyAPI — Configuration for OpenClaw RLM Skill
# Port: 8317 (CLIProxyAPI default, DO NOT change unless there's a conflict)
# Docs: https://github.com/router-for-me/CLIProxyAPI

port: 8317
host: "127.0.0.1"
secret-key: "change-this-in-production"

# Enabled providers
# OAuth is configured via web interface: http://localhost:8317/management.html
providers:
  openai-codex:
    enabled: true

# Logging (optional)
# log-level: "info"
# log-file: "/tmp/cliproxyapi.log"
