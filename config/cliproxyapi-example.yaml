# CLIProxyAPI — Configuración para OpenClaw RLM Skill
# Puerto: 8317 (default de CLIProxyAPI, NO cambiar a menos que haya conflicto)
# Docs: https://github.com/router-for-me/CLIProxyAPI

port: 8317
host: "127.0.0.1"
secret-key: "cambiar-esto-en-produccion"

# Proveedores habilitados
# OAuth se configura via interfaz web: http://localhost:8317/management.html
providers:
  openai-codex:
    enabled: true

# Logging (opcional)
# log-level: "info"
# log-file: "/tmp/cliproxyapi.log"
