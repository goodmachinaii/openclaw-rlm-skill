// Fragment of ~/.openclaw/openclaw.json
//
// This file is FOR REFERENCE ONLY. The RLM skill does NOT require changes here.
// The skill invokes RLM via bash, not via OpenClaw's model system.
//
// The model configuration here is what OpenClaw already uses for the main
// agent (Telegram, etc.).

{
  "agents": {
    "defaults": {
      "model": {
        // Main model for OpenClaw agent
        "primary": "openai-codex/gpt-5.3-codex",

        // Fallbacks if primary is unavailable
        "fallbacks": ["openai-codex/gpt-5.2"]
      }
    }
  }
}
